import React, { Fragment } from 'react';
import styled from 'styled-components';


function ProgressBar(props) {
    return (
           
            <h2>Question {props.currentQ} of {props.totalQ}</h2>


    );
}

export default ProgressBar;

